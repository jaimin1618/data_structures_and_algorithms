#include<bits/stdc++.h>
using namespace std;

struct Point {
    int x, y;
    Point(int X, int Y): x(X), y(Y) {};    
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    

    return 0;
}