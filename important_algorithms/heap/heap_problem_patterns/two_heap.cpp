#include<bits/stdc++.h>
using namespace std;

/**
 * Q: Get median of Streaming array [4, 7, 3, 5, 1, ...upcoming];
 * A: using Two Heap method for this
 */

double get_median(vector<int>& v) {
	

	return 0;
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);


	vector<int> v {4, 7, 3, 5, 1};
	cout << get_median(v) << '\n';
	

	return 0;
}