#include<bits/stdc++.h>
using namespace std;

/**
 * Key points of this concept
 * - If array is sorted then BS will be applied definately
 * - if Not Sorted, there is concept of BSA - binary search on answer
 * 
 * - Here, we may need to develop CRITERIA (key == v[mid]) to return answer
 * - and CRITERIA to move left or right
 * - but similar to BS, here we also divide problem in half 
 */

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	

	return 0;
}